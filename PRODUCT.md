# GIRAF

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 주 사용자는 천문학 수업에서 IRAF를 배우는 학생입니다.
- 학생이 실제 관측 파일을 선택하고 IRAF task의 파라미터를 설정하여 영상 처리와 결과 확인을 수행하는 상황을 지원합니다.
- 주 사용자가 학생이라는 사실이 제품을 특정 수업이나 고정된 실습 순서에 한정하지는 않습니다.

## Product Purpose

GIRAF는 IRAF의 패키지와 task를 탐색하고, 파일과 파라미터를 선택하여 실제 IRAF 계산을 실행하는 로컬 웹 작업 환경입니다.

제품의 성공 기준은 사용자가 필요한 task를 찾아 실행하고, 입력·설정·결과의 관계를 확인하며, 결과를 다음 작업에 재사용할 수 있는 것입니다. 원본 보존과 실행 과정을 추적할 수 있는 기록을 유지합니다.

## Positioning

IRAF 계산을 별도의 근사 구현으로 대체하지 않고, 설치된 IRAF CL 또는 PyRAF를 웹 인터페이스에서 다룹니다. 패키지와 task, 파라미터라는 IRAF의 작업 단위를 유지하면서 파일 선택, 실행 기록, 영상 확인, 결과 재사용을 연결합니다.

## Operating Context

- 현재 로컬 단일 사용자용이며 서버는 `127.0.0.1`에 바인딩합니다. IRAF 본체의 별도 설치가 필요합니다.
- 현재 화면은 한국어이며 IRAF 패키지명, task명, 파라미터명은 원래 식별자를 사용합니다.
- 주요 흐름은 패키지/task 탐색 → 폴더 또는 이전 결과에서 입력 선택 → 파라미터와 생성 예정 출력 확인 → 실행 → 결과 확인·다운로드·다음 입력으로 재사용입니다.
- 여러 폴더의 파일 선택과 선택 묶음 저장을 지원합니다. 입력 개수와 처리 순서를 고정하지 않으며 결합 결과를 다시 결합할 수 있습니다.
- task별 파라미터를 저장하고, 관련 CCD task에서 처리 설정과 기기 헤더 매핑을 공유합니다.
- 현재 구현은 `web/`의 React·TypeScript·Vite 화면과 `giraf/`의 Python API로 구성됩니다. 실행 진입점은 `uv run main.py`이며 기본 주소는 `http://127.0.0.1:8501/`입니다. 프런트엔드 개발 서버는 `web/`에서 `bun run dev`로 실행하고 `/api`를 Python 서버로 연결합니다.

## Capabilities and Constraints

### 현재 구현 범위

- 내장 영상 뷰어는 단일 Primary HDU의 2D FITS 영상을 표시합니다. 범용 실행에는 1D 스펙트럼 FITS와 노드 스키마의 좌표·측광 텍스트 파일도 사용할 수 있습니다.
- 설치된 패키지 선언(`.cl`)과 파라미터 파일(`.par`)을 정적으로 탐색해 task 목록과 파라미터 폼을 생성합니다. 패키지 설정과 재귀 pset 참조도 편집하며, 탐색 과정에서 CL 스크립트를 실행하지 않습니다.
- 기존 전용 CCD task 외의 작업은 설치된 `.par`에서 노드 스키마·GUI·워크플로우 입출력을 자동 생성합니다. 작업별 JSON은 필수가 아니며 특수 의미를 보완하는 외부 override만 선택적으로 사용합니다. `IRAF_EXTENSIONS.md`를 기준으로 확인합니다.
- 자동 생성은 모든 작업의 과학적 결과 검증을 뜻하지 않습니다. 모호한 파일 의미는 추론 근거를 보존하고 실제 누락된 pset 등 설치 문제는 구체적으로 표시합니다.
- 내장 영상 뷰어에서 확대·이동·표시 범위 조절·A/B 비교·픽셀 조사를 제공합니다.
- `imexamine`의 지원 측정과 프로파일은 실제 IRAF가 계산하고 GKI 그래픽을 SVG로 표시합니다.
- 실행별로 입력 사본과 독립된 `uparm`을 사용합니다. `ccdproc`와 `ccdhedit`도 사본에서 실행합니다.
- 입력과 원본 경로·해시, 파라미터, 실행 명령, 엔진 정보, 로그 및 출력 대응을 실행 폴더에 기록합니다.
- 파일 목록의 추정 분류와 실제 FITS 헤더를 구분해야 합니다. `ccdtype`는 실제 IRAF 필터이며 입력이 조건과 다르면 건너뛸 수 있습니다.
- CL/PyRAF 결과 동등성은 IRAF 버전·입력 헤더·파일 순서·파라미터 등 실행 조건을 맞춰 비교해야 합니다.

### 현재 제한과 미정 사항

- 임의 CL 명령 입력, 대화형 overscan fitting, 일부 고급 `combine` 옵션은 현재 지원 범위 밖입니다.
- 측광은 비대화형 `phot`/`daofind`, 분광은 `sarith` 연산을 자동 생성 스키마로 실행합니다. 스펙트럼 전용 뷰어, 대화형 aperture 추출·`splot`, 파장 보정 database와 특수 커서 작업은 추가 구현이 필요합니다. 영상 이동·확대 계약이 영상 정렬 전체 절차를 자동화한다는 뜻은 아닙니다. 저장소의 별도 처리 스크립트를 앱 기능으로 소개하지 않습니다.
- 추가 패키지 경로와 선택적 JSON override를 등록할 수 있지만 패키지·바이너리·의존성 설치는 별도로 필요합니다. 정적 탐색에서 해석되지 않는 동적 CL 등록과 `.par` 없는 procedure 파라미터는 현재 탐색 대상이 아닙니다.
- Windows/WSL 실행은 현재 검증되지 않았습니다.
- 이후 추가할 task별 계약·어댑터, 플랫폼, 우선순위와 일정은 미정입니다. 현재 제한을 영구적인 제품 금지 사항으로 해석하지 않습니다.
- 추가 언어, 제품별 접근성 요구, 원격·다중 사용자 운영 요구는 아직 확정되지 않았습니다.

## Brand Commitments

- 제품 이름은 기존 문서와 화면의 `GIRAF`를 사용합니다.
- 새로운 시각적 방향이나 별도 브랜드 약속은 이번 초기화에서 정하지 않았습니다.

## Evidence on Hand

- `README.md`: 현재 기능, 실행 방법, 지원 task, 재현 기록 및 제한 사항.
- `giraf/task_catalog.py`, `giraf/task_parameters.json`: 등록된 task와 IRAF 파라미터 정의. 파라미터는 IRAF 2.18.1 초기값 스냅샷입니다.
- `IRAF_EXTENSIONS.md`, `giraf/task_discovery.py`, `giraf/task_schema.py`, `giraf/generic_tasks.py`: 설치 패키지의 정적 탐색, parameter 기반 스키마 생성, 범용 실행과 현재 제한입니다. 실제 CL/PyRAF 통합 검증 범위는 `imcopy`, `imarith`, `gauss`, `imtranspose`, `phot`, `sarith`이며 전체 IRAF 호환성을 뜻하지 않습니다.
- `tests/`: 실제 IRAF 실행, CL/PyRAF 비교, 보정, 결과 생성 및 원본 보존 등을 검사하는 기존 코드. 테스트의 존재를 이번 작업에서 통과를 확인한 것으로 표현하지 않습니다.
- `observations/`: 로컬 관측 자료. `runs/`: 기존 로컬 실행 기록과 결과.
- 고객 사례, 사용자 평가, 성능 수치 및 전체 IRAF 호환성은 별도의 근거 없이 주장하지 않습니다.

## Product Principles

1. IRAF를 배우는 학생이 실제 task와 파라미터를 이해하고 다룰 수 있도록 지원합니다.
2. 특정 수업의 고정된 절차에 제품을 가두지 않고, 범용 IRAF 작업 환경으로의 확장을 지향합니다.
3. 실제 IRAF 계산의 의미를 보존하고 입력·설정·출력의 관계를 확인할 수 있게 합니다.
4. 원본 자료를 보존하고 실행을 추적·재현할 수 있는 기록을 유지합니다.
5. 현재 검증된 기능과 장기적인 지향점을 구분하여 설명합니다.
