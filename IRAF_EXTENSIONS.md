# IRAF parameter에서 노드 만들기

GIRAF는 설치된 패키지 선언(`*.cl`)과 parameter 파일(`*.par`)을 읽어 작업, 편집 폼, 워크플로우 포트를 생성합니다. 새 작업을 추가할 때 Python/React 구현이나 작업별 JSON 목록을 작성할 필요가 없습니다. 작업 식별자는 `noao.digiphot.apphot.phot`처럼 전체 패키지 경로를 포함합니다. 저장된 CCD 워크플로우의 짧은 식별자는 기존 어댑터에서 유지합니다.

## 하나의 스키마가 연결하는 경로

`패키지 선언 → .par 파싱 → 노드 스키마 → GUI / 입력 검증 / 워크플로우 연결 / CL·PyRAF 실행`

- 파서는 이름, 타입, 모드, 기본값, 선택지, 범위, 설명을 보존합니다. 두 종류의 인용부호, 여러 줄 설명, 인용된 공백, 명시적 빈 기본값과 생략된 기본값, Fortran 지수·육십진 수치를 구분합니다. 잘못된 행을 조용히 버리지 않고 파일 위치와 함께 보고합니다.
- boolean은 스위치, enum은 선택 메뉴, 숫자·문자열은 해당 입력 필드가 됩니다. GUI는 필수 여부, IRAF 설명과 수치 범위를 표시합니다. 숨김 모드 `h`는 IRAF의 질의 동작을 뜻하며 GUI에서 파라미터를 제거하라는 의미가 아닙니다.
- 파일 parameter는 종류와 방향, 단일/복수 입력, 단일/입력별 출력 정보를 갖는 포트가 됩니다. 이미지, 스펙트럼, 텍스트, 마스크, 숫자 상수 대체 입력, 리스트 값 스트림과 커서 명령 스트림, GKI metacode, binary를 구분합니다. 빈 값이 기본인 숨김 출력은 선택 사항이며 파일 이름을 입력했을 때만 생성·수집합니다.
- 패키지 설정과 재귀 pset은 같은 파서와 폼을 사용합니다. 다른 디렉터리의 유일한 pset이나 선언된 별칭도 찾습니다. 해석할 수 없는 전역 간접 기본값은 문자열로 덮어쓰지 않고 IRAF가 실행 시 해석하도록 보존합니다.
- 워크플로우는 이 포트의 파일 종류, 개수, 출력 역할을 그대로 사용합니다. 파일 출력이 없는 작업의 표준 출력도 텍스트 포트이며 실행 로그와 구분됩니다.

## 파일 의미의 추론과 근거

IRAF `.par`에는 입출력 방향을 명시하는 독립 컬럼이 없습니다. `s`는 일반 문자열에도 파일명에도 쓰이고, `f`만으로 FITS와 텍스트를 구분할 수 없습니다. 따라서 타입과 parameter 설명의 공통 표현에서 파일 의미를 추론합니다. 예를 들어 `Input images`는 복수 영상 입력이고 `Title of output image`는 문자열 설정입니다. 작업 이름별 분기나 수백 개의 수동 목록은 사용하지 않습니다.

스키마의 `schemaSource`와 `ioEvidence`에 근거가 남습니다. 각 파라미터의 원본 `.hlp` 정의에서 복수 개수와 추가 출력, 접두사 명명 규칙을 보완합니다. 설치된 SPP 소스 또는 parameter 해시가 일치하는 `task_schemas.json`의 소스 분석 결과가 있으면 확인된 I/O 역할을 보완합니다. 캐시와 소스가 없어도 parameter 기반 생성은 작동합니다. 일반 문자열의 파일 의미를 알 수 없다는 이유로 작업 전체를 비활성화하지 않습니다.

자동 생성과 모든 작업의 과학적 결과 검증은 다릅니다. 설명이 모호한 파일, 동적 파일명, database 디렉터리, 화면 장치 등의 의미는 `.par`만으로 완전히 결정할 수 없습니다. 추론한 포트는 확인 가능한 기본 연결이며, 이런 특수 의미는 선택적 외부 override나 실행 어댑터로 보완할 수 있습니다. 없는 pset처럼 실제 설치 정의가 불완전한 경우에는 해당 참조를 구체적으로 표시합니다.

## 실행과 결과

영상 입력의 파일 선택기에서는 FITS와 여러 `.list` 파일을 함께 선택할 수 있습니다. 이 처리는 task 이름이 아닌 공통 입력 종류와 복수 입력 스키마를 따릅니다. 목록의 상대 경로는 해당 목록 파일의 폴더를 기준으로 해석하며, 중첩 `@목록`, 영상 section과 순서를 보존하고 영상 template 확장은 IRAF `sections`에 맡깁니다. 단일 영상 포트는 펼친 결과가 한 개인지 검증합니다. 좌표나 커서 등 다른 종류의 입력을 영상 목록으로 펼치지는 않습니다.

`workflow` 패키지의 `image_list`는 선택하거나 연결한 영상으로 `.list` 파일을 만드는 워크플로우 도구입니다. 기존 공통 노드와 설정 UI를 사용하며, 결과에는 원본 영상의 절대 경로를 한 줄씩 기록합니다. 여러 목록 노드의 출력을 같은 복수 영상 입력에 연결할 수 있고 직접 선택한 파일과 함께 전달됩니다. 검증 후 목록이나 원본 영상이 변경되면 실행을 중단합니다.

입력 파일을 실행 폴더로 복사하고 독립된 `uparm`을 사용합니다. 비대화형 실행의 기본 `interactive`, `verify`, `update`는 `no`이며 IRAF의 원래 기본값도 스키마에 보존합니다. 커서 parameter는 명령 파일을 연결할 수 있으며 실시간 화면 커서 세션을 대신하지는 않습니다.

복수 입력과 출력은 native IRAF 목록으로 한 번 호출합니다. 출력 개수 때문에 입력별로 task를 반복 호출하지 않으므로 공통 영역 계산과 공유 그래픽 스트림이 유지됩니다. 단일 보조 입력은 공유하고 서로 맞지 않는 입력 개수는 실행 전에 검증합니다. 접두사 출력은 목록이 아니라 접두사를 전달하고 실제 생성 이름으로 수집합니다. 결과 이름 충돌은 실행 전에 거부합니다.

실행 manifest에 스키마와 parameter를 고정하고 패키지·입력 파일 해시를 다시 확인합니다. CL/Python 명령, 실제 parameter, 로그, 결과 해시가 기록에 남습니다. IRAF 오류 또는 필요한 출력 파일 누락은 실패입니다. 로그는 `$log`, 연결 가능한 표준 출력은 `$stdout` 역할을 가지므로 후속 작업에서 혼동되지 않습니다. 실행 폴더는 운영체제 샌드박스가 아닙니다.

실제 CL/PyRAF 통합 테스트는 영상 복사·연산·필터·전치, `phot`, `sarith`, `blkavg → imstack`, `scopy`, `phot → txdump`를 포함합니다. `.par`만 사용하는 경로도 기존 영상·측광·분광 통합 테스트로 확인합니다.

## 새 패키지

표준 `iraf$pkg`, `iraf$noao`, `iraf$extern`을 탐색합니다. 외부 패키지는 다음처럼 루트만 추가하고 작업 목록을 새로고침합니다.

```sh
GIRAF_IRAF_PACKAGE_ROOTS=/data/iraf/mypackage uv run main.py
```

여러 경로는 OS 경로 구분자(macOS/Linux는 `:`)로 연결합니다. 추가 루트에는 패키지 이름과 같은 `.cl` 파일과 `package` 선언이 있어야 합니다. 패키지·바이너리·의존성 설치는 별도로 필요합니다. 현재 정적 발견은 `.par`가 있는 task를 대상으로 하며, 동적으로 계산되는 CL 등록이나 procedure 안에만 정의된 parameter는 별도 발견 기능이 필요합니다.

예외적인 파일 의미를 수정하려면 `GIRAF_TASK_DESCRIPTORS`에 `version: 1`, `tasks` 객체를 갖는 JSON 경로를 지정할 수 있습니다. 각 작업의 `inputs`, `outputs`, `fixed`를 설정하는 선택 기능이며 일반 작업을 활성화하는 필수 단계가 아닙니다. 입출력에는 실제 parameter 이름과 `image`, `text`, `mask`, `metacode`, `binary` 종류를 사용합니다.

파일 변경은 **작업 목록 새로고침**으로 반영하고, 환경변수를 바꾼 경우 서버를 재시작합니다. 이미 저장된 노드는 값을 보존하므로 정의 변경 후 새 노드를 추가해 비교할 수 있습니다.

## 스트림과 복수 출력 확인

- `imalign.list`: 원본 CL에서 첫 사용 전에 임시 목록으로 할당되는 숨김 리스트 변수입니다. 입력 포트와 GUI에서 제외하고 `internalParameters` 및 근거를 보존합니다.
- `phot.icommands/gcommands`: `*imcur/*gcur` 커서 스트림입니다. 재생 파일은 `[x y wcs] key [command]`이며 좌표 표와 구분해 검증합니다. `\040` 같은 IRAF 키 이스케이프와 좌표 없는 명령도 받습니다. scalar `imcur/gcur`는 파일 포트로 만들지 않습니다.
- `phot`: 입력 두 영상에 측광 표 두 개와 공유 `plotfile` GKI 한 개를 CL/PyRAF 양쪽에서 생성·수집했습니다. GKI는 텍스트로 읽지 않으며 다운로드와 형식별 포트 연결을 제공합니다.
- `daofind`: 좌표 표, `starmap`, `skymap`의 세 역할을 두 영상에 대해 CL/PyRAF 양쪽에서 수집했습니다. 뒤 두 역할의 원본 명명 규칙은 접두사입니다.
- `allstar`의 세 역할과 `psf`의 네 역할도 원본 도움말에서 스키마로 생성합니다. 이 두 task의 과학적 실행 검증은 이번 통합 테스트 범위에 포함하지 않았습니다.
- `imalign`의 공통 trim은 CL에서 검증했습니다. 현재 설치의 PyRAF에서는 원본 `imcopy("copy.fits[1:62,1:64]", "copy.fits")`만 독립 실행해도 IRAF `bus error`가 재현됩니다. 해당 PyRAF 통합 테스트는 실패 상태로 남겨 두었으며, 다른 backend로 몰래 전환하거나 trim을 생략하지 않습니다.
